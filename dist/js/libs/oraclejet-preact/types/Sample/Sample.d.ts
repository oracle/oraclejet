import { h } from "preact";
declare type Props = {
    /**
     * Use this property to set the greeting text.
     */
    preferredGreeting?: string;
};
export declare function Sample({ preferredGreeting }: Props): h.JSX.Element;
export {};
